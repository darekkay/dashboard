import React from "react";
import { render, screen } from "@testing-library/react";

import { widgetContentProps {{#if useSagas}}, widgetStatusDisplay {{/if}} } from "common/utils/mock";

import {{ properCase name }} from "../index";

describe("<{{ properCase name }} />", () => {
  test("renders without errors", () => {
    render(
      <{{ properCase name }}
        {...widgetContentProps}
      />
    );
    expect(screen.getByRole("TODO", { name: /TODO text/i })).toBeInTheDocument();
  });
{{#if useSagas}}
  test("renders a widgetStatusDisplay when available", () => {
    render(
      <{{ properCase name }}
        {...widgetContentProps}
        widgetStatusDisplay={widgetStatusDisplay}
      />
    );
    expect(screen.getByTestId("widget-status-display")).toBeInTheDocument();
  });
{{/if}}
});
