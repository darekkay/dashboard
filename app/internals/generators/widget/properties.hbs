import { WidgetProperties } from "widgets/index";

{{#if configurationEnabled}}import { WidgetOptions } from "./configuration";{{/if}}

export default {
  // TODO: adjust properties and run yarn scan:widgets
  widgetType: "{{kebabCase name}}",
  category: "general", // TODO: check category
  configurable: {{configurationEnabled}},
  initialHeight: 1,
  initialWidth: 1,
  initialOptions: {},
  initialMeta: {},
} as WidgetProperties{{#if configurationEnabled}}<WidgetOptions>{{/if}};
