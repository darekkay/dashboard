import type { WidgetProperties } from "widgets/index";

{{#if configurationEnabled}}import type { WidgetOptions } from "./configuration";{{/if}}

const properties: WidgetProperties{{#if configurationEnabled}}<WidgetOptions>{{/if}} = {
  // TODO: adjust properties and run yarn scan:widgets
  widgetType: "{{kebabCase name}}",
  category: "general", // TODO: check category
  configurable: {{configurationEnabled}},
  hasSaga: {{useSagas}},
  initialHeight: 1,
  initialWidth: 1,
  initialOptions: {},
  initialMeta: {},
};

export default properties;
