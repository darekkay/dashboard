import { WidgetProperties } from "widgets/index";

export default {
  widgetType: "text",
  category: "general",
  configurable: false,
  initialHeight: 4,
  initialWidth: 4,
  initialOptions: {},
  initialMeta: {},
} as WidgetProperties;
