import { Widgets } from "./index";
export default {
  "date-time": { configurable: false, initialHeight: 1, initialWidth: 2 },
  search: { configurable: true, initialHeight: 1, initialWidth: 2 },
  text: { configurable: false, initialHeight: 2, initialWidth: 2 }
} as Widgets;
